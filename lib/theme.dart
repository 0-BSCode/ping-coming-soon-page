import 'package:flutter/material.dart';

// Primary
const COLOR_BLUE = HSLColor.fromAHSL(1, 223, 0.87, 0.63);

// Secondary
const COLOR_PALE_BLUE = HSLColor.fromAHSL(1, 223, 1.0, 0.88);
const COLOR_LIGHT_RED = HSLColor.fromAHSL(1, 354, 1, 0.66);

// Neutral
const COLOR_GRAY = HSLColor.fromAHSL(1, 0, 0, 0.59);
const COLOR_DARK_BLUE = HSLColor.fromAHSL(1, 209, 0.33, 0.12);
